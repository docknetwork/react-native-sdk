// @ts-nocheck
import assert from 'assert';

export const validation = {};

export type InitParams = {
  substrateUrl: string,
  cheqdApiUrl?: string,
  networkId?: string,
  cheqdMnemonic?: string,
};
